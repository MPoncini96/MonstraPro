"""Secret encryption at rest.

Placeholder. Should provide device-key generation (first boot only, stored
outside the SQLite file with restrictive permissions) and
encrypt/decrypt helpers used by alpaca_credentials storage. See
ARCHITECTURE.md section 8.
"""
